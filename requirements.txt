# Core numerics
numpy>=1.23,<2.0
scipy>=1.9

# Config + plotting + logging
pyyaml>=6.0
matplotlib>=3.6
pandas>=1.5

# Testing
pytest>=7.2

#   cd ../gym-pybullet-drones
#   pip install -e .


# Multi-simulator notes
# - PyBullet backend: gym-pybullet-drones is an external sibling repo.
# - Gazebo backend: Gazebo Sim + ROS 2 Jazzy + ros_gz are system dependencies.
# - MuJoCo backend: reserved in the registry; install mujoco when implemented.
# - ArduPilot backend: reserved in the registry; the ArduPilot repo is cloned as a sibling.
